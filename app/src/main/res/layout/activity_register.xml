<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@mipmap/ic_register_background"
    tools:context=".MainActivity">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/ic_gilbert_logo"
        android:id="@+id/logo"
        android:layout_centerHorizontal="true"
        android:paddingTop="30dp"
        android:paddingBottom="30dp"/>

    <TextView
        android:id="@+id/text_register"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Discover Gilbert with your friends. Search for interesting attractions and review them for everyone to see."
        android:textColor="#ffffff"
        android:textSize="18sp"
        android:paddingTop="140dp"
        android:layout_below="@id/logo"
        android:gravity="center"
        android:lineSpacingMultiplier="1.3"
        android:textStyle="bold|italic" />

    <Button
        android:id="@+id/button_register_facebook"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_below="@id/text_register"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:textColor="#ffffff"
        android:text="Register with Facebook" />

</RelativeLayout>
